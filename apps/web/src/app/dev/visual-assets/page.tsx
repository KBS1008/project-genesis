import { VisualAssetsScreen } from '@/presentation/screens/dev/VisualAssetsScreen';

/** Developer-only visual asset manager route. */
export default function VisualAssetsPage() {
  return <VisualAssetsScreen />;
}
