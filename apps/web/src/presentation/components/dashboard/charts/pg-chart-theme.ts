/** Shared Recharts axis and grid defaults using design tokens. */
export const PG_CHART_AXIS_TICK = Object.freeze({ fill: 'var(--muted)', fontSize: 11 });
export const PG_CHART_AXIS_LINE = Object.freeze({ stroke: 'var(--border)' });
export const PG_CHART_GRID_STROKE = 'var(--color-panel)';
export const PG_CHART_LEGEND_STYLE = Object.freeze({ fontSize: 12 });
