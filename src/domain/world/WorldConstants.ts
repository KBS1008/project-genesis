/**
 * @module @domain/world/WorldConstants
 *
 * Shared world defaults aligned with official game content.
 *
 * @see game-content/worlds/world_default.yaml
 * @see game-content/regions/region_default.yaml
 */

/** Default world id from official starter content. */
export const DEFAULT_WORLD_ID = 'world_default';

/** Default region id from official starter content. */
export const DEFAULT_REGION_ID = 'region_default';
