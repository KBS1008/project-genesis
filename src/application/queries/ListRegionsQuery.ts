/**
 * @module @application/queries/ListRegionsQuery
 *
 * Input for listing all bootstrapped regions.
 */

/** Query to list all regions in deterministic order. */
export type ListRegionsQuery = Record<string, never>;
