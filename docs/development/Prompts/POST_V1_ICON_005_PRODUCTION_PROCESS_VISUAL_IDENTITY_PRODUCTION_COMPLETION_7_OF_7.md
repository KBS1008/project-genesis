# POST-V1 ICON-005 — Production / Process Visual Identity Production Completion 7/7

## Mode

BOUNDED PRODUCTION COMPLETION SLICE.

This is NOT:
- a new art-direction exploration,
- another pilot,
- a broad ProductionScreen redesign,
- a gameplay/content/balance change,
- a generic visual audit,
- an invitation to reopen sealed visual families.

The ICON-005 process-art direction has already passed the independent human visual gate.

Your job is to:

1. preserve the approved ICON-005 visual grammar,
2. promote the exact three approved pilot primaries,
3. author the exact four remaining enabled recipe primaries,
4. achieve honest 7/7 ICON-005 production coverage,
5. activate the production asset family through one authoritative resolver,
6. integrate it narrowly into the real ProductionScreen,
7. validate the result in actual runtime at desktop and narrow viewport,
8. update Scenario-B accounting honestly,
9. return ONE final close candidate,
10. STOP.

Follow:

`docs/development/CURSOR_IMPLEMENTATION_GUIDE.md`

Do not commit, push, or tag.

---

# 1. Human Visual Authority — FROZEN

The independent human gate has approved the ICON-005 pilot.

Record the following as authoritative for this slice:

- ICON-005 Process Art Direction — APPROVED / PASS / SEALED
- ICON-005 Visual Contract — APPROVED as Production Authority
- detailed process vignette grammar — APPROVED
- exact three existing pilot primaries — APPROVED FOR PROMOTION
- production activation — authorized by THIS bounded slice
- ProductionScreen integration — authorized ONLY within the narrow scope defined below

Do NOT reopen the approved art direction unless hard technical evidence shows that the approved assets themselves are unusable.

Do NOT regenerate an approved pilot merely to make it newer or more stylistically uniform.

---

# 2. Baseline / Repository Hygiene

At the beginning:

- record current HEAD,
- record branch,
- inspect `git status`,
- distinguish task-owned changes from unrelated working-tree churn,
- preserve all unrelated user work.

The previous review baseline was historically around:

`b5f3348`

but DO NOT assume HEAD is still that commit.

Use the actual current repository state.

The predecessor pilot may already exist as committed or uncommitted work. Determine its real state before modifying anything.

Do not absorb unrelated shell/dashboard/dev/evidence churn into this task.

---

# 3. Existing Authorities

Treat these tracks as SEALED / FROZEN:

- ICON-001 resources
- ICON-002 building categories
- ICON-003 building visual identity — 23/23
- ICON-004 technology/research visual identity — 22/22
- World Visual Presentation Slice 1
- menu scenic art
- BR-001
- previous building/infrastructure art contracts

They may be referenced for visual differentiation.

They must not be redesigned or modified.

---

# 4. ICON-005 Semantic Contract

The approved semantic question is:

> ICON-005 answers:
> “What industrial process / transformation is happening?”

It does NOT answer:

- where it happens → ICON-003
- what resource exists → ICON-001
- what capability was researched → ICON-004

Preserve this distinction throughout production activation.

The production primary must depict:

- transformation,
- fabrication,
- processing,
- assembly,
- or active industrial handling

rather than merely showing:

- a building exterior,
- a finished resource,
- a research apparatus,
- a warehouse still-life,
- or a generic industrial machine with no process cue.

---

# 5. Approved Production Visual Grammar

Production authority:

`docs/design/production/PRODUCTION_PROCESS_ICON_005_VISUAL_CONTRACT.md`

Update the document status through version history without deleting previous pilot history.

Final production authority status should become equivalent to:

`APPROVED / PRODUCTION AUTHORITY`

and, if this slice fully passes independent-close prerequisites:

`PRODUCTION 7/7 CLOSE CANDIDATE`

Do NOT mark the overall track independently SEALED inside the implementation itself.

Independent ChatGPT review performs the final seal.

Preserve these approved invariants:

- stylized detailed industrial game art
- process-focused 3/4 vignette
- active/recent transformation
- compact industrial platform/composition
- strong central silhouette
- dark-UI-compatible industrial lighting
- real transparency
- no baked UI
- no readable text
- no logos
- no fake labels
- no pseudo-writing
- no watermarks
- no people by default
- no floating resource badges
- no infographic arrows between resource icons
- no full facility exterior as hero subject

Target master:

- 1024×1024
- RGBA
- true alpha

Primary inspection:

- 96 px
- 128 px

Reward/detail inspection:

- 256 px

Expected runtime presentation:

approximately 64–96 px where the existing ProductionScreen structure safely permits it.

---

# 6. Exact Enabled Recipe Inventory

Re-audit the authoritative current enabled recipe YAML before creating assets.

Historical inventory contains exactly seven enabled recipes:

1. `recipe_planks`
2. `recipe_advanced_planks`
3. `recipe_steel`
4. `recipe_machine_parts`
5. `recipe_industrial_machinery`
6. `recipe_advanced_electronics`
7. `recipe_consumer_goods`

Verify this against current repository content.

If content changed materially:

STOP only if the change makes the exact 7/7 scope invalid.

Do not silently invent coverage for newly added recipes.

---

# 7. Exact Three Approved Pilot Promotions

Promote, without unnecessary regeneration:

- `recipe_planks`
- `recipe_steel`
- `recipe_advanced_electronics`

Existing approved pilot masters should be reused as the production-art source if technical QA still passes.

Expected pilot source family:

`docs/design/production/pilot-icon-005/process-primary/`

The pilot manifest historically identifies exactly these three.

Promotion must NOT count as newly authored art.

Each approved pilot counts exactly once as an independent authored concept in Scenario-B accounting.

Do not count:

- pilot copy,
- production copy,
- PNG derivative,
- WebP derivative,
- evidence board inclusion

as additional concepts.

---

# 8. Exact Four New Production Primaries

Create exactly FOUR new detailed ICON-005 primaries:

- `recipe_advanced_planks`
- `recipe_machine_parts`
- `recipe_industrial_machinery`
- `recipe_consumer_goods`

Do not create extra recipe art.

Do not regenerate the three approved pilots unless a genuine technical defect requires repair.

All four new assets must meet the same production quality bar as the approved three.

---

# 9. recipe_advanced_planks — Mandatory Differentiation

This is the highest known semantic duplication risk.

It MUST NOT look like:

`recipe_planks` with minor prop changes.

The existing approved `recipe_planks` image establishes basic wood cutting / plank production.

For `recipe_advanced_planks`, inspect the authoritative YAML semantics, prerequisites, inputs, outputs, facility compatibility, and research dependencies.

Create a visually more advanced processing identity only where supported by content.

Possible visual dimensions MAY include, if semantically supported:

- more controlled feed system,
- multi-stage processing,
- finishing/precision handling,
- advanced cutting/planing arrangement,
- mechanized material transfer.

Do NOT invent unsupported product types or gameplay mechanics.

Acceptance test:

At 96–128 px a player should be able to distinguish:

`recipe_planks`

from:

`recipe_advanced_planks`

without relying on text.

If this cannot be achieved honestly from content semantics, stop with a genuine semantic blocker rather than fabricating mechanics.

---

# 10. recipe_machine_parts

Inspect authoritative recipe semantics first.

Target visual identity:

precision transformation of metal/material into machine components.

The image should communicate an active manufacturing step, not:

- a machine shop building,
- a pile of finished parts,
- or a generic research machine.

Use semantically supported process cues such as machining/forming/precision fabrication only where justified by repository content.

It must remain visually distinct from:

- ICON-003 `machine_shop`
- relevant ICON-004 manufacturing technologies
- ICON-001 `machine_parts`

---

# 11. recipe_industrial_machinery

This should communicate heavier assembly / construction of industrial machinery.

Do not merely depict:

- an industrial machinery resource icon enlarged,
- a complete factory,
- or another electronics assembly line.

The process should visibly involve assembly/integration of machinery-scale components where semantically supported.

At 96–128 px the silhouette should differ materially from:

- `recipe_machine_parts`
- `recipe_advanced_electronics`
- `recipe_steel`

---

# 12. recipe_consumer_goods

This is a hybrid/general assembly risk.

Audit the actual recipe inputs/outputs and compatible buildings before art generation.

The art must show a plausible productive transformation supported by those semantics.

Avoid inventing a specific consumer product category if the content does not define one.

Prefer a semantically neutral but visually active manufacturing/assembly/packaging process where appropriate.

Do NOT turn the art into:

- retail shelves,
- a warehouse,
- a supermarket,
- a logistics depot,
- or a pile of finished boxes.

It still must read as a process.

---

# 13. Family-Level Differentiation

At completion, all seven ICON-005 primaries must read as one family without becoming repetitive.

Required distinction:

- wood/basic process
- wood/advanced process
- heavy thermal process
- precision machine-part process
- heavy machinery assembly
- precision electronics assembly
- consumer/hybrid production process

Do not solve family coherence by making all seven compositions identical.

Do not solve differentiation by random camera angles.

Use:

- apparatus silhouette,
- transformation type,
- material interaction,
- process motion implication,
- scale of machinery,
- controlled material cues

while preserving the approved common visual grammar.

---

# 14. Technology-vs-Process Firewall

ICON-004 is SEALED 22/22.

The new process art must not become “research apparatus art with another label”.

For relevant recipes, compare visually against the related ICON-004 primary.

Ask:

> Does this image show a capability being represented,
> or material actually being transformed/assembled?

ICON-005 must remain the latter.

No ICON-004 production assets may be modified.

---

# 15. Building-vs-Process Firewall

Compare each recipe primary against compatible `buildingTypes` from authoritative recipe content.

ICON-003 answers:

> where can this happen?

ICON-005 answers:

> what operation is happening?

A process primary may contain industrial apparatus.

It must not primarily read as the exterior/identity of a placeable building.

No ICON-003 asset may be modified.

---

# 16. Resource-vs-Process Firewall

Where inputs/outputs correspond to ICON-001 resources:

resource material may appear physically inside the vignette.

Do not paste ICON-001 icons into the scene.

Do not make the output resource itself the hero subject.

The transformation apparatus/process must remain the primary read.

No ICON-001 asset may be modified.

---

# 17. Generated-Art QA — Mandatory

Inspect every new master manually and programmatically.

Reject/repair:

- readable accidental text
- fake logos
- pseudo-writing
- watermarks
- baked checkerboard
- white matte
- black matte
- edge halos
- impossible disconnected machinery where visually obvious
- malformed tools
- duplicated impossible components
- broken conveyors
- floating machinery
- unexplained geometry
- obvious generation artifacts

Beautiful but semantically dishonest art is NOT acceptable.

Semantically correct but visibly low-quality placeholder art is NOT acceptable.

Repair task-local failures before returning the close candidate.

---

# 18. Alpha / Technical QA

All seven production masters must pass:

- expected dimensions
- RGBA
- real alpha
- non-trivial transparent area
- no checkerboard bake
- no visible halo on dark UI
- valid load/decode

Required result:

`7 / 7 PASS`

Create/update an authoritative production QA report under the appropriate production asset path.

Do not rely only on visual inspection.

---

# 19. Runtime Derivatives

Create runtime-safe derivatives following existing repository conventions.

Prefer the existing visual asset pipeline.

Do not invent a second image-loading architecture.

Derivatives may include PNG/WebP where existing architecture expects them.

Scenario-B accounting rule:

master + runtime derivative = ONE authored concept.

Never double-count formats.

---

# 20. Production Manifest

Create or promote an authoritative production manifest for ICON-005.

It must cover exactly the seven enabled recipes.

Each entry should make clear:

- recipe ID
- master path
- runtime path(s)
- status
- alpha QA result
- source/provenance where repository convention requires it

The three promoted pilots must be identifiable as approved pilot promotions rather than newly authored concepts.

The four new ones must be identifiable as new production concepts.

---

# 21. Registry / Resolver

Integrate ICON-005 through ONE authoritative production mapping/resolver following current asset architecture.

Desired semantic hierarchy:

`recipeId`
→ ICON-005 detailed process primary if production-approved
→ safe existing fallback
→ generic production/process fallback

Do NOT duplicate recipe→asset maps in multiple components.

Use the existing:

`visual-asset-registry.ts`

and related loader/resolver architecture where appropriate.

Create a reusable presentation primitive if consistent with current architecture, e.g.:

`ProductionProcessVisual`

or equivalent.

Do not force that exact name if repository conventions indicate a better one.

---

# 22. Defensive Fallback

Even after 7/7 enabled recipe coverage, preserve defensive behavior for:

- unknown recipe ID,
- future recipe without ICON-005,
- missing/corrupt asset resolution.

The UI must not crash.

Do not display a giant empty image slot.

Do not leak raw asset paths.

Do not silently map an unknown recipe to an unrelated known process.

---

# 23. ProductionScreen Integration — AUTHORIZED BUT BOUNDED

This slice MAY integrate ICON-005 into the real ProductionScreen.

This is not permission for a ProductionScreen redesign.

Find the current real recipe/process presentation surface.

Integrate the process primary where it materially improves recognition.

Preferred behavior:

- process art visible near recipe/process identity,
- player-facing recipe name remains primary readable text,
- controls remain usable,
- status/progress remains readable,
- resource quantities remain understandable,
- no unnecessary extra cards,
- no giant hero redesign,
- no new gameplay interaction.

Target visual size:

approximately 64–96 px in catalog/list/job contexts where current layout supports it.

If the current structure safely supports a larger process preview without redesign, use judgment.

Do not stretch the asset into decorative background art.

---

# 24. Do Not Confuse Process Art With Resource Icons

ProductionScreen may already use ICON-001 for inputs/outputs.

Preserve those roles.

A valid row may therefore contain:

- ICON-005 = process identity
- ICON-001 = input/output resource identity

That is intentional.

Do not replace useful resource icons with the process primary.

Do not duplicate the process primary multiple times in the same row without reason.

---

# 25. Process State / Progress

Existing process/job state remains UI-owned.

Do not bake into artwork:

- progress bars
- paused state
- blocked state
- completion badges
- warning badges
- percentage text

If an existing runtime process/job card can reuse ICON-005 cleanly, that is allowed within bounded integration.

Do not invent a new production flow merely to showcase the art.

---

# 26. Player-Facing Text

Use authoritative player-facing recipe names already provided by application/content architecture.

Do not introduce raw recipe IDs into visible UI.

Do not create new duplicated frontend translation/name maps if authoritative content-name resolution already exists.

If existing raw-ID leakage is discovered outside the exact integration touched by this slice, document it rather than expanding scope unless the defect is directly caused by this task.

---

# 27. Required Evidence — Art Family

Generate:

`docs/architecture/reviews/evidence/ICON_005_PRODUCTION_7_OF_7_FAMILY_BOARD.png`

Must show all seven production primaries at useful comparison scale with authoritative player-facing names or clearly mapped IDs in evidence metadata.

Also generate:

`docs/architecture/reviews/evidence/ICON_005_PRODUCTION_7_OF_7_SCALE_BOARD.png`

Show all seven at minimum:

- 96
- 128
- 256

The board must allow human inspection of readability and reward quality.

---

# 28. Required Cross-Family Evidence

Generate:

`docs/architecture/reviews/evidence/ICON_005_PRODUCTION_CROSS_FAMILY_DIFFERENTIATION_BOARD.png`

Include representative comparisons against:

- ICON-001 resource
- ICON-003 compatible building
- ICON-004 related technology

Do not need every possible permutation.

The board must demonstrate that ICON-005 has its own semantic visual role.

---

# 29. Mandatory Wood Differentiation Evidence

Generate:

`docs/architecture/reviews/evidence/ICON_005_PLANKS_ADVANCED_PLANKS_DIFFERENTIATION.png`

Show:

- `recipe_planks`
- `recipe_advanced_planks`

at:

- 96
- 128
- 256

This is a mandatory acceptance artifact.

Human reviewer must be able to see that these are two distinct processes.

---

# 30. Runtime Evidence

Capture the REAL ProductionScreen after integration.

Required:

`docs/architecture/reviews/evidence/ICON_005_PRODUCTION_RUNTIME_DESKTOP.png`

approximately:

`1440 × 900`

and:

`docs/architecture/reviews/evidence/ICON_005_PRODUCTION_RUNTIME_NARROW.png`

approximately:

`480 × 900`

Where possible, evidence should visibly contain multiple recipe/process identities.

Runtime evidence must verify:

- ICON-005 actually resolves in the application,
- no broken images,
- no gray lazy placeholders,
- no giant empty slots,
- player-facing names remain readable,
- controls remain usable,
- process art does not destroy information density,
- resource information remains understandable,
- narrow layout remains usable.

Static mocks are NOT sufficient for final completion.

---

# 31. Runtime Asset Resolution Check

Add/extend automated verification that proves:

- all 7 enabled recipe IDs resolve to ICON-005 production primaries,
- all 7 referenced files exist,
- all runtime paths decode/load,
- unknown recipe ID uses safe fallback,
- no duplicate recipe mapping,
- no pilot/dev-only path is accidentally used at runtime.

Required:

`7 / 7 production resolution PASS`

---

# 32. Full Coverage Matrix

The final close report must contain a seven-row matrix:

| Recipe ID | Player Name | Process Class | Master | Runtime | Registry | Resolver | ProductionScreen | Alpha | Scale | Semantic QA | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|

No recipe may be omitted.

Final desired result:

`7 / 7 ACTIVE`

Anything less is not production completion.

---

# 33. Visual Quality Gate for Four New Assets

For each of the four new primaries explicitly assess:

1. beautiful enough for Scenario-B production quality?
2. detailed enough at 128–256?
3. readable at 96?
4. clearly depicts an industrial process?
5. distinct from the other six ICON-005 images?
6. distinct from matching ICON-003 building?
7. distinct from related ICON-004 technology?
8. more than an enlarged ICON-001 resource?
9. no generated-art defect?
10. semantically honest to current content?

Repair obvious task-local defects before close candidate.

Do not defer obvious bad art to a future batch.

There is no future ICON-005 batch planned if 7/7 succeeds.

---

# 34. Root Tests / Asset Tests

Run relevant focused tests plus full root gates.

Mandatory:

`pnpm typecheck`

PASS.

`pnpm lint`

PASS with 0 errors.

`pnpm test`

PASS.

`pnpm build:web`

PASS.

Asset gates:

- dimensions: 7/7 PASS
- RGBA/alpha: 7/7 PASS
- checkerboard/matte: 7/7 PASS
- manifest integrity: PASS
- registry integrity: PASS
- resolver: 7/7 PASS
- unknown fallback: PASS
- runtime asset load: 7/7 PASS

Add focused tests for new resolver/presentation behavior where appropriate.

Do not weaken tests to make the slice pass.

---

# 35. Scenario-B Accounting

Update:

`docs/design/GAME_ART_VISUAL_CONTENT_MASTER_INVENTORY.md`

Current historical ledger before production activation identifies ICON-005 as:

- 3 pilot concepts
- 0/7 production active
- human gate pending

After successful completion, update honestly.

Accounting:

- promoted `recipe_planks` pilot = 1 authored concept total
- promoted `recipe_steel` pilot = 1 authored concept total
- promoted `recipe_advanced_electronics` pilot = 1 authored concept total
- `recipe_advanced_planks` = +1 new authored concept
- `recipe_machine_parts` = +1 new authored concept
- `recipe_industrial_machinery` = +1 new authored concept
- `recipe_consumer_goods` = +1 new authored concept

ICON-005 family total:

`7 independent Tier-1 authored process concepts`

Do NOT count:

- runtime derivatives,
- evidence boards,
- pilot copies,
- production copies,
- registry entries,
- resolver code,
- screenshots

as authored art.

Scenario-B planning envelope remains:

~380–520 authored deliverables + ~12 procedural systems

and remains a planning envelope, NOT a quota.

Do not claim Scenario B overall complete.

---

# 36. Inventory Final State

If all completion gates pass, update Production family status from pilot/gap to equivalent of:

`ACTIVE — ICON-005 7/7`

Dependency note for ProductionScreen should state that ICON-005 process primaries are production-active.

Do not change unrelated domain priorities merely because ICON-005 closes.

---

# 37. Contract History

Update:

`docs/design/production/PRODUCTION_PROCESS_ICON_005_VISUAL_CONTRACT.md`

Preserve the existing pilot history.

Add version-history entries for:

- human visual approval
- production-authority promotion
- 3 approved pilot promotions
- four new production primaries
- 7/7 activation
- ProductionScreen bounded integration
- production-completion close candidate

Do not rewrite history as if the assets were always production authority.

---

# 38. Firewalls

STRICTLY OUT OF SCOPE:

- recipe gameplay changes
- recipe input/output changes
- recipe duration changes
- balance
- economy
- simulation semantics
- technology/research changes
- building changes
- world-map changes
- direct placement
- Player Guidance workstream
- milestone workstream
- employee/workforce visuals
- transport visuals
- menu scenics
- broad ProductionScreen redesign
- navigation redesign
- new recipes
- new resource types
- generic shell cleanup
- unrelated responsive redesign

Do not opportunistically fix unrelated issues.

---

# 39. Consolidated Definition of Done

This slice is a final close candidate only when ALL are true:

- approved ICON-005 grammar preserved
- exact three approved pilots promoted
- no unnecessary pilot regeneration
- exact four missing recipe primaries authored
- all seven masters production quality
- `recipe_planks` vs `recipe_advanced_planks` clearly differentiated
- 7/7 alpha PASS
- 7/7 dimensions PASS
- generated-art QA PASS
- 7/7 manifest coverage
- 7/7 registry/resolver coverage
- unknown fallback PASS
- real ProductionScreen integration complete
- desktop runtime evidence PASS
- narrow runtime evidence PASS
- no broken/gray image slots
- player names remain readable
- ICON-001 resource semantics preserved
- ICON-003 building semantics preserved
- ICON-004 technology semantics preserved
- no gameplay/content/balance changes
- Scenario-B accounting updated without double counting
- inventory accurately says ICON-005 7/7 ACTIVE
- contract history updated
- root typecheck PASS
- root lint PASS with 0 errors
- root tests PASS
- root build:web PASS
- no unrelated scope absorbed

Do not return a premature close candidate with known task-local visual or runtime defects.

Repair them within this bounded slice.

---

# 40. Stop Conditions

STOP and report instead of guessing only if one of these occurs:

1. authoritative recipe content no longer contains the expected seven-recipe scope;
2. `recipe_advanced_planks` cannot be honestly differentiated from `recipe_planks` without inventing unsupported semantics;
3. one of the approved pilot files fails asset integrity and cannot be repaired without changing approved art direction;
4. ProductionScreen architecture makes bounded integration impossible without a material UX redesign;
5. a required change would alter gameplay/content/balance semantics;
6. unexpected cross-scope regression requires reopening a sealed track;
7. a genuine architecture ambiguity prevents one authoritative resolver.

Normal implementation friction is NOT a stop condition.

Missing evidence is NOT a reason to stop if it can be captured in this slice.

A weak generated image is NOT a reason to stop; regenerate/repair it.

---

# 41. Required Close-Candidate Report

Create:

`docs/architecture/reviews/POST_V1_ICON_005_PRODUCTION_PROCESS_VISUAL_IDENTITY_PRODUCTION_COMPLETION_7_OF_7_CLOSE_CANDIDATE.md`

Include:

## A. Executive Summary
## B. Baseline / HEAD / Working Tree
## C. Human Approval Authority
## D. Contract Promotion
## E. Current Recipe Inventory
## F. Three Pilot Promotions
## G. Four New Production Primaries
## H. recipe_planks vs recipe_advanced_planks
## I. Seven-Recipe Semantic Matrix
## J. Alpha / Dimensions / Generated-Art QA
## K. Family Coherence
## L. Building-vs-Process Differentiation
## M. Technology-vs-Process Differentiation
## N. Resource-vs-Process Differentiation
## O. Production Manifest
## P. Registry / Resolver
## Q. Defensive Fallback
## R. ProductionScreen Integration
## S. Desktop Runtime Evidence
## T. Narrow Runtime Evidence
## U. 7/7 Runtime Resolution
## V. Automated Tests
## W. Root Gates
## X. Scenario-B Accounting
## Y. Master Inventory Update
## Z. Firewalls
## AA. Remaining Known Issues
## AB. Exact Task-Owned Files
## AC. Final Decision

For visual assets include exact paths.

For runtime evidence include exact screenshot paths.

For all gates include actual command and actual result.

---

# 42. Final Decision — EXACTLY ONE

Return exactly one:

### OPTION A — ICON-005 PRODUCTION COMPLETION 7/7 FINAL CLOSE CANDIDATE READY

Use only if ALL DoD items pass.

### OPTION B — ONE BOUNDED REPAIR REQUIRED

Use when the remaining defect is local and clearly repairable.

State exact defect and exact repair boundary.

### OPTION C — SEMANTIC ART BLOCKER

Use only if one of the remaining recipes cannot be represented honestly under current authoritative content.

### OPTION D — PRODUCTION INTEGRATION REQUIRES MATERIAL UX / ARCHITECTURE DECISION

Use only if bounded runtime integration genuinely cannot be completed without a broader decision.

Do not invent additional options.

---

# 43. Git / Delivery Rule

Do NOT:

- stage,
- commit,
- push,
- tag.

Leave the completed close candidate in the working tree for independent review.

Do not ask whether to commit instead of completing required runtime evidence.

If browser/runtime evidence tooling requires a local dependency such as Playwright browser installation and it is safe/task-local, install/use the required local runtime tooling according to repository policy and capture the evidence.

Only stop if installation would require an unsafe or material environment change.

---

# 44. Final Core Rule

ICON-005 already has an approved art direction.

Do not redesign it.

Promote exactly:

- recipe_planks
- recipe_steel
- recipe_advanced_electronics

Create exactly:

- recipe_advanced_planks
- recipe_machine_parts
- recipe_industrial_machinery
- recipe_consumer_goods

Then prove:

**7/7 production coverage.**

The finished family must make Production feel like an industrial game rather than a text administration screen.

ICON-005 must answer:

> “What transformation is happening here?”

while:

- ICON-001 answers what material/resource it is,
- ICON-003 answers where production happens,
- ICON-004 answers what capability was researched.

Integrate the art into the real ProductionScreen without redesigning the screen.

No fake coverage.
No duplicated accounting.
No empty image slots.
No raw asset leakage.
No gameplay changes.
No reopening sealed tracks.

Return one final close candidate and STOP.